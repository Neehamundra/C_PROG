#include<stdio.h>
int main(){
    //program to calculate the are of square
    float side;
    printf("side is :");
    scanf("%f",&side);

    printf("area of square is:%f",side*side);
    return 0;
}