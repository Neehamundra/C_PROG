#include<stdio.h>//preprocessing directives
int main(){//starting of function
    int a=5;
    char star='*';//; is like a full stop
    float pi=3.14;
    int _age=25;//no comma and space is allowed in varaible
    _age=26;//the value of the variable can be change
    /*constant are of three types they are int constantlike 1,2,3,-1,-2,real constant i.e 1.0,2.0,3.14,and character constant i.e 'a','$','A'
    */
   printf("This program includes the basic learing of c\n");//printf is the output function which can print the given statement or the value by using the format specifiers
   printf("The _age is:%d\n",_age);
   printf("value of pi:%f\n",pi);
   printf("star looks like this:%c\n",star);
   return 0; 


}