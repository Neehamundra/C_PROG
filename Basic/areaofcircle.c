#include<stdio.h>
int main(){
    //calculate the area of circle
    float radius;
    printf("radius is:");
    scanf("%f",&radius);

    printf("radius of a circle is:%f",3.14*radius*radius);//formula to calculated the radius of a circle is pi r2
    return 0;
}